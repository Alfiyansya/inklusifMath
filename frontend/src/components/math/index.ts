/**
 * Math components barrel export.
 *
 * MathRenderer — core MathJax wrapper with aria-label narasi verbal
 * MathDisplay  — dual-layer (visual + a11y) for student/teacher pages
 */

export { MathRenderer } from "./MathRenderer";
export type { MathRendererProps } from "./MathRenderer";

export { MathDisplay } from "./MathDisplay";
export type { MathDisplayProps } from "./MathDisplay";
